apiVersion: v1
kind: Service
metadata:
  name: {{ include "syncit.fullname" . }}
  labels:
    {{- include "syncit.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "syncit.selectorLabels" . | nindent 4 }}
